# 给 Agent 的旧版升级提示词

已经装过旧版“行业调研分析”Skill 的用户，可把下面整段发给当前使用的本地 Agent。此指令授权在核对和备份后升级 Skill；不授权删除备份、覆盖业务项目、上传客户资料或修改其他客户端。

---

请把我当前使用的“行业调研分析”Skill 升级到以下公开仓库的 `0.6.0` 图表门禁版，并完成真实安装验收，不要只给我操作教程。如果仓库 `main` 已不再是 `0.6.0`，先告诉我版本差异，不擅自安装另一个版本：

`https://github.com/tushigan/industry-research-analysis-skill`

请按顺序执行：

1. 先确认当前客户端和它实际使用的技能发现路径。列出这个客户端能发现的所有 `anqian-dongcha-baogao`、`anqian-dongcha-baogao-v2` 入口，读取各入口的 `SKILL.md`、`VERSION.json`（若有）、文件指纹和本地改动。不要假设某个目录是唯一入口，也不要改动其他客户端的安装。
2. 从上述 GitHub 仓库获取最新代码，核对仓库地址、`main` 提交号、`anqian-dongcha-baogao/SKILL.md` 和 `VERSION.json`。先阅读仓库根目录的 `README.md`、`安装与MCP配置.md` 和 `scripts/安装Skill.py`。运行仓库提供的公开内容扫描。若来源不符、扫描失败、旧入口有无法辨认的个人修改，先停下并告诉我差异，不覆盖。
3. 升级前，把每个旧入口完整备份到**技能发现路径之外**，记录原路径、备份路径、版本、指纹和恢复方法。不要把备份仅改名留在 `skills/` 目录，也不要删除我的研究资料和历史报告。仓库安装器的 `--force` 会为同名稳定入口自动备份；其他入口仍须逐一处理。
4. 在当前客户端的正式技能目录安装仓库中的单一稳定入口 `anqian-dongcha-baogao`。Codex、Claude Code 或已核实目录的其他本地 Agent，优先用仓库的 `scripts/安装Skill.py` 和对应 `--agent`/`--dest` 参数；同名旧入口确认已备份后使用 `--force`。OpenClaw 使用其当前版本的原生安装、更新和查询命令，不猜测安装路径。若此前还有 `-v2` 入口，将其备份并移出技能发现路径，避免新旧两份同时触发；不删除备份。
5. 回读**实际安装目录**的 `SKILL.md`、`VERSION.json` 和必需脚本，确认名称为 `anqian-dongcha-baogao`、版本与仓库一致、图表方案预检和图表占比检查器齐全。运行 `node scripts/检查运行环境.cjs`。优先复用已有 Playwright/Chromium 和 PyMuPDF；缺依赖时按仓库说明安装免费运行依赖，不改其他项目的环境。隔离安装 Python 后，按安装器输出设置 `ANQIAN_PYTHON`。
6. 从实际安装目录运行包内测试，并用临时匿名项目做浏览器、讲者台和 PDF 的端到端验收：`RUN_DELIVERY_TESTS=1 node --test tests/完整交付.test.cjs`。如运行时不可用、测试失败或被跳过，不得写“升级完成”；先修复或说明卡点。不要把匿名样本写进业务项目，也不要把旧版验收记录冒充本轮结果。
7. 在新会话中确认客户端只发现一个新版入口，并做一次真实触发测试。Tavily MCP 若原本可用，不重复注册；若不可用，分别说明配置、工具发现和真实搜索的状态，不把 MCP 问题伪装成 Skill 安装成功。
8. 最后用中文汇报：升级前后版本、仓库提交号、当前唯一入口、每份备份及恢复位置、运行时与端到端测试结果、客户端是否实际发现新版，以及未通过或未验证事项。区分技术验收、图表比例检查、内容审校和业务批准；这次升级不代表任何客户报告已获批准。

如果无法确认旧版的真实位置、无法备份或遇到用户自定义改动，停止覆盖并向我说明需要决定的具体差异。不要为了完成指令跳过备份、放宽测试或上传客户文件。

---
